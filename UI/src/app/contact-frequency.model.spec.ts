import { ContactFrequency } from './contact-frequency.model';

describe('ContactFrequency', () => {
  it('should create an instance', () => {
    expect(new ContactFrequency()).toBeTruthy();
  });
});
